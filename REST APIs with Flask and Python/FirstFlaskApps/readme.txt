# What I learned:

1. Use the decorator @app.route(/linkhere)
2. Import and use render_template to call out a specific html file
3. Import jsonify and send dictionaries in json to the browser (.get_json() / return jsonify(dictionary)
4. <string:name>
5. Use Postman to test out the requests
